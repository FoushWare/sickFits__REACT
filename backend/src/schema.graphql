# import * from './generated/prisma.graphql'

# type Dog {
#   name: String!
# }

type Mutation {
  createItem(
    title: String
    description: String
    price: Int
    image: String
    largeImage: String
  ): Item!
}
type Query {
  items: [Item]!
}
